//go:build amd64

#include "textflag.h"

TEXT ·enterVexarcABI(SB), NOSPLIT, $0-40
	PUSHQ BP
	PUSHQ BX
	PUSHQ SI
	PUSHQ DI
	PUSHQ R12
	PUSHQ R13
	PUSHQ R14
	PUSHQ R15
	MOVQ entry+0(FP), AX
	MOVQ slotsBase+8(FP), BX
	MOVQ thread+16(FP), DI
	MOVQ frame+24(FP), SI
	MOVQ exit+32(FP), DX
	CALL AX
	POPQ R15
	POPQ R14
	POPQ R13
	POPQ R12
	POPQ DI
	POPQ SI
	POPQ BX
	POPQ BP
	RET
